# 작품 이미지 업로드 & 리사이징 처리 

## ✅ 요구사항

### 작품등록시 썸네일 이미지 업로드
<img src="../images/upload_creation.png" width="770" alt="작품 등록 썸네일 이미지 업로드" />

- 작품 썸네일 이미지는 홈/목록/상세 등 노출과 모바일,웹 등의 환경을 고려 → 대표 이미지(포스터형, 가로형)와 기타이미지 6장으로 나눠짐
- 작품 썸네일 이미지는 각각 별도의 이미지 업로드가 가능하며 업로드시 지정된 사이즈로 자동 리사이징
- 가로형 이미지 등록시 기타이미지 6장은 자동 리사이징 → 이후 기타이미지는 각각 다른 이미지로 수정 가능
- 이미지 업로드 완료시 사용자에게 미리보기로 보여줘야 하며 업로드 대기 최소화
- 이미지 업로드/리사이징 실패시 재처리 가능해야 하며 운영자가 원인을 추적할 수 있어야 함

---

## ⚠️ 이슈사항
1. 이미지 업로드 처리는 백엔드 서버에서 처리하면 스케일/비용면에서 비효율적
2. 이미지 리사이징은 어떻게 처리할 것인가?
3. 이미지 업로드/리사이징 실패시 재처리
4. 업로드 후 이미지 첫 노출 레이턴시(Latency) 최적화

---

## 🛠️ 해결방법

1. 이미지 업로드 처리는 백엔드 서버에서 처리하면 스케일/비용면에서 비효율적
   -  프론트엔드에서 업로드 처리: presigned URL 사용, S3 bucket에 이미지 업로드

    <br>
   
2. 리사이징은 어떻게 처리할 것인가?
   - 프론트엔드에서 리사이징 처리 But, 가로형 이미지 등록시 '6장 이미지 리사이징 처리' 필요
   - 가로형 이미지 등록시 '6장 이미지 리사이징 처리' 방법은?
     - 해결 방법 1: 마찬가지로 프론트엔드에서 처리
     - 해결 방법 2: AWS Lambda 처리
     - 결론: '해결방법 2' 선택
     - 이유:

    <br>

3. 이미지 업로드/리사이징 실패시 재처리

    <br>

4. 업로드 후 이미지 첫 노출 레이턴시(Latency) 최적화
   - S3 직접 접근 대신 CDN 캐시(CloudFront)를 통해 첫 로딩과 이후 재방문 속도 개선 

---

## ☁️ 최종 Architecture & Flow
<img src="../images/architecture.png" width="800" alt="이미지 업로드 아키텍쳐" />

1. Client → SpringBoot: presigned URL 발급 요청
2. SpringBoot → S3: presigned URL 생성(서명)
3. Client → S3: presigned URL로 원본 이미지 PUT 업로드
4. (Trigger) S3 이벤트 → SQS 적재(Resize Queue)
   - 리사이징 실패시 3번 시도 후 안되면 Resize DLQ에 적재
   - 이후 CloudWatch → SNS → Lambda(Slack으로 실패 메세지 전송)
5. SQS → Lambda: 리사이징 작업 실행(6개의 파생 이미지 생성)
6. Lambda → S3: 파생 이미지 저장
7. Lambda → SpringBoot: 처리 완료 후 백엔드로 콜백(HMAC 인증) → SSE로 완료 알림
    - 백엔드 콜백 실패시 Callback Fail Queue에 적재
8. Client → CloudFront → S3: CDN 캐시 기반 이미지 제공

---

## 📌 기타 고민사항
1. 리사이징 완료 통지 방식을 어떻게 할것인가? 
   - Polling vs SSE(WebSocket 포함)
2. 

---

## 🚀 성능 효과 / 지표

---

## 🔧 개선점

